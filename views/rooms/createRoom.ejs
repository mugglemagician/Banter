<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/createRoom.css">
    <link
        href="https://fonts.googleapis.com/css2?family=Kanit:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
        rel="stylesheet">
    <title>Create Room</title>
</head>

<body>

    <main>

        <% if ((success && success.length> 0) || (error && error.length > 0)) { %>

            <div class="flashContainer">

                <div class="flashContent">

                    <% if (success && success.length> 0) { %>
                        <span class="success">
                            <%= success %>
                        </span>

                        <% } %>

                            <% if (error && error.length> 0) { %>
                                <span class="error">
                                    <%= error %>
                                </span>

                                <% } %>

                </div>

                <span class="closeFlash">
                    &cross;
                </span>

            </div>

            <% } %>

                <form action="/rooms" method="POST">

                    <h1 class="createRoom">Create Room</h1>

                    <div class="inputContainer">
                        <div>
                            <label for="name">Room Name : </label>
                        </div>
                        <input type="text" id="name" required name="name" autocomplete="off" maxlength="10">
                    </div>

                    <div class="inputContainer">
                        <div>
                            <label for="password">Password : </label>
                        </div>
                        <input type="password" id="password" name="password" autocomplete="off">
                    </div>

                    <button class="btn">Create</button>
                    <a href="/rooms">Back</a>

                </form>

    </main>

    <script src="/scripts/closeFlash.js"></script>
</body>

</html>